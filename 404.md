---
layout: default
permalink: /404.html
title: "Page not found"
description: "That page doesn't exist on The DESI Sikka. Jump back to the latest crypto news, browse a category, or search the archive."
robots: "noindex, follow"
sitemap: false
---

<div class="meta-strip compact">
  <span>Oops</span>
  <span>404</span>
</div>

<section class="section shell">
  <header class="page-intro">
    <span class="mono-label">Lost in the ledger</span>
    <h1 class="page-title">This page ran off with the roadmap and never came back.</h1>
    <p class="page-sub">The link is dead, moved, or never existed. Good news: the news desk is still open.</p>
  </header>

  <div class="chip-row" style="margin-top:2rem;" aria-label="Popular destinations">
    <a class="chip" href="{{ '/' | relative_url }}">Latest news</a>
    {% for cat in site.site_categories %}
      <a class="chip" href="{{ '/category/' | append: cat.slug | relative_url }}">{{ cat.name }}</a>
    {% endfor %}
    <a class="chip" href="{{ '/pages/search/' | relative_url }}">Search all stories</a>
    <a class="chip" href="{{ '/archive/' | relative_url }}">Full archive</a>
    <a class="chip" href="{{ '/pages/contact/' | relative_url }}">Report a broken link</a>
  </div>

  <p class="empty-note" style="margin-top:2rem;">Typed it yourself? Check the URL for a typo. Followed a link? Tell us on the <a href="{{ '/pages/contact/' | relative_url }}">contact page</a> and we'll fix it.</p>
</section>